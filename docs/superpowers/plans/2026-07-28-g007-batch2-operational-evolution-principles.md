# G007 Batch 2 Operational and Evolution Principles Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish PR-06 through PR-08 as one governed decision-oriented principles batch, verify the exact Stage A deployment, and close only those three topics with durable Stage B evidence.

**Architecture:** Each principle remains an independent MDX knowledge unit using the canonical nine-section contract. Front matter is the canonical relationship input, the source ledger governs factual provenance, generated projections are written only by the content generator, and delivery uses a test-first Stage A content commit followed by an exact-deployment-backed Stage B closure commit.

**Tech Stack:** Bun command runner, Node test runner through `bun test`, MDX, Mermaid, JSON source ledger, Docusaurus 3.10.2, TypeScript 6, GitHub Pages.

## Global Constraints

- Execute in a new worktree `/Users/seal/projects/tego-arch/.worktrees/g007-principles-batch2` on branch `codex/g007-principles-batch2`, created from the current `main` that contains design commit `ef81c8e`.
- Do not reuse or rewrite the historical `codex/g007-principles` Batch 1 branch.
- Publish only PR-06 through PR-08 at `/principles/pr-06` through `/principles/pr-08`.
- Use the exact H2 order: `学习问题`, `要保护的性质`, `冲突与适用上下文`, `机制`, `误用与反原则`, `适用尺度`, `相邻原则`, `说明性场景`, `来源`.
- Every page asks 3–5 learning questions, cites at least two governed sources from independent domains, has exactly one manifest-primary citation, distinguishes source fact/inference/site analysis, and contains an original Mermaid diagram or decision table.
- Do not add visible links to unpublished PR-09 through PR-17.
- Do not manually edit files under `src/generated/`; run `bun run generate:content`.
- During Stage A, keep PR-06 through PR-08 unchecked in `docs/content-backlog.md`.
- Use only facts summaries for the six new all-rights-reserved sources; do not copy source diagrams, tables, examples, or book structure.
- Use Bun commands for repository scripts and tests.
- G007 remains in progress after this batch; do not checkpoint it.

## File Structure

### Create

- `tests/g007-batch2-content.test.mjs` — real-content, relationship, source, and concept-correction contract.
- `content/principles/pr-06-kiss-yagni-dry.mdx` — KISS/YAGNI/DRY decision page.
- `content/principles/pr-07-fail-fast-fail-safe-graceful-degradation.mdx` — failure-response decision page.
- `content/principles/pr-08-evolutionary-design.mdx` — evolutionary design and migration page.
- `docs/reviews/g007-batch2.md` — Stage B deployment and review evidence.
- `tests/g007-batch2-deployment.test.mjs` — immutable deployment-closure contract.

### Modify

- `data/source-ledger.json` — six new source records and three governed document entries.
- `data/source-link-health.json` — factual observations for the six new transports.
- `content/principles/pr-01-information-hiding.mdx` — reciprocal PR-06 and PR-08 relations.
- `content/principles/pr-02-cohesion-coupling.mdx` — reciprocal PR-06 and PR-07 relations.
- `content/principles/pr-04-dip-ioc-dependency-injection.mdx` — reciprocal PR-07 and PR-08 relations.
- `content/principles/pr-05-composition-over-inheritance.mdx` — reciprocal PR-06 and PR-08 relations.
- `tests/g007-batch1-content.test.mjs` — preserve Batch 1 coverage after intentional published relations expand.
- `tests/content-review-health.test.mjs` — source/document count fixture after Stage A generation.
- `tests/project-status.test.mjs` — document/source counts in Stage A and completed topics in Stage B.
- `tests/source-ledger-pagination.test.mjs` — governed-source pagination fixture.
- `tests/source-ledger-rendering.test.mjs` — governed-source count fixture.
- `docs/content-backlog.md` — Stage B closure only.

### Generated by `bun run generate:content`

- `src/generated/project-status.json`
- `src/generated/source-ledger.json`
- `src/generated/topic-indexes.json`
- `src/generated/topic-manifest.json`

Other generated files may change only if the generator deterministically emits them from the accepted source/content inputs. Review every generated diff before committing.

---

### Task 1: Create the failing G007 Batch 2 content contract

**Files:**

- Create: `tests/g007-batch2-content.test.mjs`
- Read: `tests/g007-batch1-content.test.mjs`
- Read: `scripts/content-metadata.mjs`
- Read: `scripts/content-relations.mjs`
- Read: `scripts/source-ledger.mjs`

**Interfaces:**

- Consumes: `findMarkdownHeadings(source)`, `readContentDocuments(root)`, `extractInternalLinks(document)`, and `extractExternalLinks(document)`.
- Produces: A real-repository contract that later source, content, and generation tasks must satisfy.

- [ ] **Step 1: Create the exact topic, heading, and relationship fixtures**

Create `tests/g007-batch2-content.test.mjs` with:

```js
import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';
import test from 'node:test';
import {fileURLToPath} from 'node:url';

import {
  findMarkdownHeadings,
  readContentDocuments,
} from '../scripts/content-metadata.mjs';
import {extractInternalLinks} from '../scripts/content-relations.mjs';
import {extractExternalLinks} from '../scripts/source-ledger.mjs';

const contentRoot = fileURLToPath(new URL('../content/', import.meta.url));
const expected = new Map([
  ['PR-06', ['principles/pr-06-kiss-yagni-dry.mdx', '/principles/pr-06']],
  [
    'PR-07',
    [
      'principles/pr-07-fail-fast-fail-safe-graceful-degradation.mdx',
      '/principles/pr-07',
    ],
  ],
  ['PR-08', ['principles/pr-08-evolutionary-design.mdx', '/principles/pr-08']],
]);
const h2 = [
  '学习问题',
  '要保护的性质',
  '冲突与适用上下文',
  '机制',
  '误用与反原则',
  '适用尺度',
  '相邻原则',
  '说明性场景',
  '来源',
];
const relationships = new Map([
  ['PR-06', ['PR-01', 'PR-02', 'PR-05', 'PR-08']],
  ['PR-07', ['PR-02', 'PR-04', 'QA-01']],
  ['PR-08', ['PR-01', 'PR-04', 'PR-05', 'PR-06', 'MTH-03', 'MTH-04']],
]);
const solePrimary = new Map([
  ['PR-06', 'src-martin-fowler-yagni-2015'],
  ['PR-07', 'src-aws-rel05-bp01-graceful-degradation'],
  ['PR-08', 'src-martin-fowler-parallel-change-2014'],
]);
const routeByTopic = new Map([
  ['PR-01', '/principles/pr-01'],
  ['PR-02', '/principles/pr-02'],
  ['PR-04', '/principles/pr-04'],
  ['PR-05', '/principles/pr-05'],
  ['PR-06', '/principles/pr-06'],
  ['PR-08', '/principles/pr-08'],
  ['QA-01', '/quality-attributes/qa-01'],
  ['MTH-03', '/methods/mth-03'],
  ['MTH-04', '/methods/mth-04'],
]);

const [documents, manifest, ledger] = await Promise.all([
  readContentDocuments(contentRoot),
  readFile(new URL('../src/generated/topic-manifest.json', import.meta.url), 'utf8').then(JSON.parse),
  readFile(new URL('../data/source-ledger.json', import.meta.url), 'utf8').then(JSON.parse),
]);
const byId = new Map(
  documents
    .filter(({metadata}) => typeof metadata.topic_id === 'string')
    .map((document) => [document.metadata.topic_id, document]),
);
const topics = new Map(manifest.topics.map((topic) => [topic.id, topic]));

function requiredDocument(id) {
  const document = byId.get(id);
  assert.ok(document, `${id} must be published`);
  return document;
}

function section(body, heading) {
  const headings = findMarkdownHeadings(body).filter(({level}) => level === 2);
  const index = headings.findIndex(({text}) => text === heading);
  assert.notEqual(index, -1, `missing ## ${heading}`);
  const start = body.indexOf('\n', headings[index].offset);
  const end = headings[index + 1]?.offset ?? body.length;
  return body.slice(start === -1 ? end : start + 1, end);
}
```

- [ ] **Step 2: Add the shared content and representation test**

Append:

```js
test('publishes PR-06 through PR-08 with the principle contract', () => {
  for (const [id, [file, slug]] of expected) {
    const document = requiredDocument(id);
    assert.equal(document.file, file);
    assert.equal(document.metadata.slug, slug);
    assert.equal(document.metadata.content_type, 'principle');
    assert.equal(document.metadata.priority, 'P0');
    assert.equal(document.metadata.status, 'reviewed');
    assert.deepEqual(document.metadata.adjacent_topics, relationships.get(id));
    assert.deepEqual(
      document.headings.filter(({level}) => level === 2).map(({text}) => text),
      h2,
    );
    const questions = section(document.body, '学习问题')
      .split(/\r?\n/u)
      .filter((line) => /^ {0,3}[-*+]\s+\S.*[?？]\s*$/u.test(line));
    assert.ok(questions.length >= 3 && questions.length <= 5, `${id} learning questions`);
    assert.match(
      document.body,
      /```mermaid[\s\S]*?```|^\|.+\|\n\|(?:\s*:?-{3,}:?\s*\|)+/mu,
      `${id} original representation`,
    );
    assert.match(document.body, /\*\*来源事实：\*\*/u, `${id} fact label`);
    assert.match(document.body, /\*\*推断：\*\*/u, `${id} inference label`);
    assert.match(document.body, /\*\*本站分析：\*\*/u, `${id} site-analysis label`);
    assert.match(document.body, /不应|不适用|反例|误用/u, `${id} negative boundary`);
    assert.equal(topics.get(id)?.published, true, `${id} manifest publication`);
  }
});
```

- [ ] **Step 3: Add source and visible relationship assertions**

Append:

```js
test('governs sources and visible Batch 2 relationships', () => {
  for (const [id, [file]] of expected) {
    const document = requiredDocument(id);
    const governed = ledger.documents[`content/${file}`];
    assert.ok(governed, `${id} governed ledger entry`);
    assert.ok(governed.citations.length >= 2, `${id} has at least two sources`);
    const primary = governed.citations.filter(({manifest_primary}) => manifest_primary);
    assert.equal(primary.length, 1, `${id} has exactly one manifest primary`);
    assert.equal(primary[0].source_id, solePrimary.get(id), `${id} primary identity`);
    const visibleExternal = new Set(extractExternalLinks(document));
    const domains = new Set();
    for (const citation of governed.citations) {
      assert.ok(visibleExternal.has(citation.citation_url), `${id} visible ${citation.source_id}`);
      domains.add(new URL(citation.citation_url).hostname);
    }
    assert.ok(domains.size >= 2, `${id} independent source domains`);
    const links = new Set(extractInternalLinks(document));
    assert.ok(links.has('/principles'), `${id} links parent index`);
    for (const adjacent of relationships.get(id)) {
      assert.ok(links.has(routeByTopic.get(adjacent)), `${id} visibly links ${adjacent}`);
    }
    assert.ok([...links].some((link) => link.startsWith('/cases/')), `${id} links a case`);
    assert.equal(
      [...links].some((link) => /^\/principles\/pr-(?:0[9]|1[0-7])$/u.test(link)),
      false,
      `${id} must not link unpublished principles`,
    );
  }
});
```

- [ ] **Step 4: Add page-specific misconception tests**

Append:

```js
test('keeps Batch 2 decisions distinct from their slogans', () => {
  assert.match(
    requiredDocument('PR-06').body,
    /当前复杂度|未来需求证据|知识重复|同步修改|错误抽象|临时重复/u,
  );
  assert.match(
    requiredDocument('PR-07').body,
    /错误可检测|副作用|可逆|安全状态|降级|诚实|静默/u,
  );
  assert.match(
    requiredDocument('PR-08').body,
    /兼容窗口|可替换点|渐进迁移|遥测|退出条件|重新评估/u,
  );
});
```

- [ ] **Step 5: Run the new test and verify the intended RED**

Run:

```bash
bun test tests/g007-batch2-content.test.mjs
```

Expected: FAIL with `PR-06 must be published`. It must not fail because of a syntax error, missing import, malformed fixture, or wrong repository path.

- [ ] **Step 6: Commit the RED contract**

```bash
git add tests/g007-batch2-content.test.mjs
git commit -m "test: define g007 operational principle contracts"
```

---

### Task 2: Govern the Batch 2 source set before drafting conclusions

**Files:**

- Modify: `data/source-ledger.json`
- Modify: `data/source-link-health.json`
- Test: `tests/source-ledger.test.mjs`
- Test: `tests/g007-batch2-content.test.mjs`

**Interfaces:**

- Consumes: The existing source schema plus existing records `src-sei-managing-technical-debt-complex-systems-2016`, `src-sre-addressing-cascading-failures`, `src-oreilly-7eb3a574d599`, `src-martin-fowler-is-design-dead-2004`, and `src-google-aip-180`.
- Produces: Five new stable source records, one governed update to the existing AIP-180 source identity, and three pending governed document entries consumed by the MDX source sections and generator.

- [ ] **Step 1: Audit the five new source transports and the existing AIP-180 transport and rights boundaries**

Open and record the final transport, authorship, page date/version, and visible rights evidence for:

| Source ID | Canonical locator | Author/org | Fact boundary | Non-claim |
| --- | --- | --- | --- | --- |
| `src-martin-fowler-yagni-2015` | `https://martinfowler.com/bliki/Yagni.html` | Martin Fowler | YAGNI rejects capability added now only for an assumed future need and does not reject refactoring, tests, or delivery capability | Does not prove advance work is never justified |
| `src-pragprog-dry-2020` | `https://media.pragprog.com/titles/tpp20/dry.pdf` | Dave Thomas and Andy Hunt / Pearson | DRY addresses duplicate authoritative knowledge or intent, not every similar syntax fragment | Does not require abstraction across distinct change reasons |
| `src-aws-rel05-bp04-fail-fast` | `https://docs.aws.amazon.com/wellarchitected/latest/reliability-pillar/rel_mitigate_interaction_failure_fail_fast.html` | Amazon Web Services | A service that cannot successfully handle a request should fail quickly and avoid unbounded stale queues | Does not mean every exception terminates the process |
| `src-aws-rel05-bp01-graceful-degradation` | `https://docs.aws.amazon.com/wellarchitected/latest/reliability-pillar/rel_mitigate_interaction_failure_graceful_degradation.html` | Amazon Web Services | Degradation preserves named core functions with explicitly acceptable failure modes | Does not authorize misleading success or weakened integrity/security |
| `src-martin-fowler-parallel-change-2014` | `https://martinfowler.com/bliki/ParallelChange.html` | Danilo Sato / MartinFowler.com | Expand, migrate, and contract provide a compatibility window for consumer migration | Does not define a universal window length or guarantee dual-path consistency |
| `src-google-aip-180` | `https://google.aip.dev/180` | Google API Improvement Proposals | Backward compatibility includes source, wire, and semantic compatibility within the proposal's API context | Does not govern every internal interface or forced coordinated upgrade |

All MartinFowler.com and AWS records use `LicenseRef-All-Rights-Reserved`, `copyright_policy: facts-and-short-quotation`, and original summaries only unless the live audit exposes a narrower explicit policy. The Pragmatic Programmer PDF uses its visible Pearson all-rights-reserved notice. Google AIP-180 uses `CC-BY-4.0` for page content and `adapt-with-attribution`.

- [ ] **Step 2: Add five canonical source records and extend the existing AIP-180 record**

For each new row above except `src-google-aip-180`, add one record to `ledger.sources` with the exact ID and locator. Use:

```json
{
  "query_insensitive": false,
  "locator_aliases": [],
  "tombstone": null,
  "registered_at": "2026-07-28",
  "checked_at": "2026-07-28",
  "source_kind": "official-docs",
  "tier": "primary",
  "allowed_evidence_roles": [
    "definition",
    "historical-context",
    "implementation",
    "learning",
    "method"
  ],
  "license_family_grouping": "identity",
  "family_grouping_evidence_url": null,
  "link_policy": "floating",
  "expected_final_approved_at": "2026-07-28"
}
```

Set page-specific fields as follows:

- Fowler YAGNI: `published_at: "2015-05-26"`, `source_kind: "independent-blog"`, `link_policy: "stable"`, all-rights-reserved.
- Pragmatic DRY: `published_at: null`, `source_kind: "textbook"`, `link_policy: "stable"`, all-rights-reserved. Retain the audited 2020 Pearson copyright year in `version` and rights evidence, not as a publication date.
- Both AWS records: `published_at: "2024-11-06"`, `source_kind: "official-docs"`, `link_policy: "floating"`, all-rights-reserved.
- Fowler Parallel Change: `published_at: "2014-05-13"`, `source_kind: "independent-blog"`, `link_policy: "stable"`, all-rights-reserved.
- Google AIP-180: reuse the existing `src-google-aip-180` canonical identity. Add `implementation` to `allowed_evidence_roles` and narrow `usage_boundary` to the fact boundary and non-claim above; do not add another source record or alter unrelated identity/transport metadata.

For the five new records, use the exact fact boundary and non-claim in Step 1, set `expected_final_transport_locator` to the canonical locator, and write an approval note naming the exact page/date evidence checked on 2026-07-28. For existing `src-google-aip-180`, update only the required evidence role and `usage_boundary`; retain its existing identity and transport approval metadata.

- [ ] **Step 3: Add the three governed document entries**

Add:

```json
{
  "content/principles/pr-06-kiss-yagni-dry.mdx": {
    "reviewed_at": "2026-07-28",
    "copyright_checks": [
      "original-structure",
      "quotation-boundary",
      "attribution-complete",
      "illustration-rights"
    ],
    "citations": [
      {
        "source_id": "src-martin-fowler-yagni-2015",
        "citation_url": "https://martinfowler.com/bliki/Yagni.html",
        "roles": ["definition", "method"],
        "manifest_primary": true,
        "usage_mode": "facts-summary",
        "attribution_note": "Yagni, Martin Fowler",
        "modification_note": null,
        "excerpt": null,
        "quotation_reviewed": false
      },
      {
        "source_id": "src-pragprog-dry-2020",
        "citation_url": "https://media.pragprog.com/titles/tpp20/dry.pdf",
        "roles": ["definition", "historical-context"],
        "manifest_primary": false,
        "usage_mode": "facts-summary",
        "attribution_note": "The Pragmatic Programmer, The Evils of Duplication, Dave Thomas and Andy Hunt / Pearson",
        "modification_note": null,
        "excerpt": null,
        "quotation_reviewed": false
      },
      {
        "source_id": "src-sei-managing-technical-debt-complex-systems-2016",
        "citation_url": "https://www.sei.cmu.edu/library/managing-technical-debt-in-complex-software-systems/",
        "roles": ["definition", "method"],
        "manifest_primary": false,
        "usage_mode": "facts-summary",
        "attribution_note": "Managing Technical Debt in Complex Software Systems, official brochure, Carnegie Mellon Software Engineering Institute",
        "modification_note": null,
        "excerpt": null,
        "quotation_reviewed": false
      }
    ]
  },
  "content/principles/pr-07-fail-fast-fail-safe-graceful-degradation.mdx": {
    "reviewed_at": "2026-07-28",
    "copyright_checks": [
      "original-structure",
      "quotation-boundary",
      "attribution-complete",
      "illustration-rights"
    ],
    "citations": [
      {
        "source_id": "src-aws-rel05-bp04-fail-fast",
        "citation_url": "https://docs.aws.amazon.com/wellarchitected/latest/reliability-pillar/rel_mitigate_interaction_failure_fail_fast.html",
        "roles": ["definition", "method"],
        "manifest_primary": false,
        "usage_mode": "facts-summary",
        "attribution_note": "REL05-BP04 Fail fast and limit queues, AWS Well-Architected Framework",
        "modification_note": null,
        "excerpt": null,
        "quotation_reviewed": false
      },
      {
        "source_id": "src-aws-rel05-bp01-graceful-degradation",
        "citation_url": "https://docs.aws.amazon.com/wellarchitected/latest/reliability-pillar/rel_mitigate_interaction_failure_graceful_degradation.html",
        "roles": ["definition", "method"],
        "manifest_primary": true,
        "usage_mode": "facts-summary",
        "attribution_note": "REL05-BP01 Implement graceful degradation to transform applicable hard dependencies into soft dependencies, AWS Well-Architected Framework",
        "modification_note": null,
        "excerpt": null,
        "quotation_reviewed": false
      },
      {
        "source_id": "src-sre-addressing-cascading-failures",
        "citation_url": "https://sre.google/sre-book/addressing-cascading-failures/",
        "roles": ["implementation", "method"],
        "manifest_primary": false,
        "usage_mode": "facts-summary",
        "attribution_note": "Chapter 22 - Addressing Cascading Failures, Mike Ulrich / Google",
        "modification_note": null,
        "excerpt": null,
        "quotation_reviewed": false
      }
    ]
  },
  "content/principles/pr-08-evolutionary-design.mdx": {
    "reviewed_at": "2026-07-28",
    "copyright_checks": [
      "original-structure",
      "quotation-boundary",
      "attribution-complete",
      "illustration-rights"
    ],
    "citations": [
      {
        "source_id": "src-martin-fowler-parallel-change-2014",
        "citation_url": "https://martinfowler.com/bliki/ParallelChange.html",
        "roles": ["definition", "method"],
        "manifest_primary": true,
        "usage_mode": "facts-summary",
        "attribution_note": "Parallel Change, Danilo Sato / MartinFowler.com",
        "modification_note": null,
        "excerpt": null,
        "quotation_reviewed": false
      },
      {
        "source_id": "src-google-aip-180",
        "citation_url": "https://google.aip.dev/180",
        "roles": ["definition", "implementation"],
        "manifest_primary": false,
        "usage_mode": "facts-summary",
        "attribution_note": "AIP-180 Backwards compatibility, Google API Improvement Proposals",
        "modification_note": null,
        "excerpt": null,
        "quotation_reviewed": false
      },
      {
        "source_id": "src-oreilly-7eb3a574d599",
        "citation_url": "https://www.oreilly.com/library/view/building-evolutionary-architectures/9781492097532/",
        "roles": ["definition", "method"],
        "manifest_primary": false,
        "usage_mode": "facts-summary",
        "attribution_note": "Building Evolutionary Architectures, 2nd Edition, Neal Ford, Rebecca Parsons, Patrick Kua, and Pramod Sadalage / O'Reilly Media",
        "modification_note": null,
        "excerpt": null,
        "quotation_reviewed": false
      },
      {
        "source_id": "src-martin-fowler-is-design-dead-2004",
        "citation_url": "https://martinfowler.com/articles/designDead.html",
        "roles": ["definition", "historical-context", "learning"],
        "manifest_primary": false,
        "usage_mode": "facts-summary",
        "attribution_note": "Is Design Dead?, Martin Fowler",
        "modification_note": null,
        "excerpt": null,
        "quotation_reviewed": false
      }
    ]
  }
}
```

- [ ] **Step 4: Refresh link health and retain factual results**

Run:

```bash
bun run refresh:links
```

Review every changed transport. The six new canonical locators must have a current attempt. Do not hand-author `healthy`; retain the script’s actual `healthy`, `auth-required`, `retired`, redirect, or error observation. Revert unrelated timestamp-only churn if it obscures review, while preserving any newly observed factual final-transport change.

- [ ] **Step 5: Validate governance while preserving the content RED**

Run:

```bash
bun test tests/source-ledger.test.mjs
bun test tests/g007-batch2-content.test.mjs
```

Expected: source-ledger tests PASS. G007 Batch 2 remains RED only because PR-06 through PR-08 content files are absent.

- [ ] **Step 6: Commit governed source inputs**

```bash
git add data/source-ledger.json data/source-link-health.json
git commit -m "content: govern g007 operational principle sources"
```

---

### Task 3: Publish PR-06 and close accurate reciprocal links

**Files:**

- Create: `content/principles/pr-06-kiss-yagni-dry.mdx`
- Modify: `content/principles/pr-01-information-hiding.mdx`
- Modify: `content/principles/pr-02-cohesion-coupling.mdx`
- Modify: `content/principles/pr-05-composition-over-inheritance.mdx`
- Modify: `tests/g007-batch1-content.test.mjs`
- Test: `tests/g007-batch2-content.test.mjs`

**Interfaces:**

- Consumes: PR-06 source IDs and exact relation fixtures from Tasks 1 and 2.
- Produces: A published PR-06 page and reciprocal visible relation set used later by PR-08.

- [ ] **Step 1: Create PR-06 front matter and nine-section skeleton**

Use:

```yaml
---
title: KISS、YAGNI 与 DRY 的张力
slug: /principles/pr-06
content_type: principle
status: reviewed
difficulty: intermediate
analyzed_at: 2026-07-28
source_cutoff: 2026-07-28
confidence: high
domains:
  - software-architecture
agent_patterns: []
protocols: []
quality_attributes:
  - maintainability
  - modifiability
tags:
  - KISS
  - YAGNI
  - DRY
summary: 用当前复杂度、未来需求证据和知识同步成本判断何时简化、推迟能力或提取抽象。
topic_id: PR-06
priority: P0
depends_on:
  - PR-01
adjacent_topics:
  - PR-01
  - PR-02
  - PR-05
  - PR-08
related_cases:
  - /cases/micro-frontends-single-spa
related_questions: []
---
```

Add the exact nine H2 headings from the global contract.

- [ ] **Step 2: Write the PR-06 decision argument and original matrix**

In `## 机制`, include this original matrix shape and explain every row in prose:

```markdown
| 判断输入 | KISS | YAGNI | DRY | 停止/复核条件 |
| --- | --- | --- | --- | --- |
| 当前问题复杂度 | 机制是否超过当前问题 | 不决定当前机制细节 | 不决定是否同一知识 | 当前行为可清楚解释和验证 |
| 未来需求证据 | 不为想象分支增加机制 | 无当前证据则不实现能力 | 不因未来相似性提前共享 | 需求被真实场景或约束验证 |
| 知识重复 | 允许清楚的局部表达 | 允许暂不建立扩展点 | 同一权威知识不应多处独立维护 | 先确认是否同一变化原因 |
| 错误抽象成本 | 间接层会增加理解成本 | 预留能力产生携带成本 | 错误复用会把独立变化绑在一起 | 出现稳定共同变化后再提取 |
| 临时重复可逆性 | 小范围重复可保持直接 | 延迟承诺保留选择 | 以所有者和删除条件控制临时重复 | 同步修改开始频繁或遗漏 |
```

The prose must include exact labels `**来源事实：**`, `**推断：**`, and `**本站分析：**`. It must state:

- DRY concerns authoritative knowledge, not every similar code shape.
- YAGNI does not forbid refactoring, testing, observability, security, or compliance work needed now.
- KISS does not justify ignoring inherent domain or operational complexity.
- Temporary duplication can be safer than an abstraction whose variation boundary is unknown.
- Shared abstraction introduces coupling and needs an owner, stable change reason, and removal/reassessment condition.

The source section visibly links the Fowler YAGNI page, Pragmatic DRY PDF, and SEI technical-debt page without quotations.

- [ ] **Step 3: Add visible relations and the illustrative case**

The `## 相邻原则` section must visibly link PR-01, PR-02, PR-05, and PR-08. The scenario must use the published `/cases/micro-frontends-single-spa` route to show why a shared shell should not absorb hypothetical cross-application capability.

- [ ] **Step 4: Add reciprocal links to existing pages**

Update front matter:

```yaml
# PR-01 adjacent_topics
  - PR-02
  - PR-03
  - PR-04
  - PR-06
  - PR-08
  - STY-00

# PR-02 adjacent_topics
  - PR-01
  - PR-03
  - PR-05
  - PR-06
  - PR-07

# PR-05 adjacent_topics
  - PR-02
  - PR-03
  - PR-04
  - PR-06
  - PR-08
```

In each existing `## 相邻原则`, add one sentence that preserves its current argument:

- PR-01: PR-06 limits speculative abstractions; PR-08 preserves a migration seam around volatile decisions.
- PR-02: PR-06 exposes coupling caused by premature shared abstractions; PR-07 treats runtime failure propagation.
- PR-05: PR-06 delays abstraction until variation is stable; PR-08 uses replaceable composition seams during migration.

- [ ] **Step 5: Update the Batch 1 regression fixture**

In `tests/g007-batch1-content.test.mjs`, replace the relationship map with the expanded lists above. Change the unpublished-route assertion from PR-06 through PR-17 to PR-09 through PR-17:

```js
assert.equal(
  [...links].some((link) => /^\/principles\/pr-(?:0[9]|1[0-7])$/u.test(link)),
  false,
  `${id} must not link unpublished principles`,
);
```

- [ ] **Step 6: Run targeted tests and inspect the expected partial GREEN**

Run:

```bash
bun test tests/g007-batch1-content.test.mjs tests/g007-batch2-content.test.mjs
```

Expected: Batch 1 passes. Batch 2 advances past PR-06 and fails with `PR-07 must be published`.

- [ ] **Step 7: Commit PR-06 and reciprocal relations**

```bash
git add content/principles/pr-01-information-hiding.mdx \
  content/principles/pr-02-cohesion-coupling.mdx \
  content/principles/pr-05-composition-over-inheritance.mdx \
  content/principles/pr-06-kiss-yagni-dry.mdx \
  tests/g007-batch1-content.test.mjs
git commit -m "content: add simplicity and abstraction principle"
```

---

### Task 4: Publish PR-07 and PR-08, then regenerate Stage A

**Files:**

- Create: `content/principles/pr-07-fail-fast-fail-safe-graceful-degradation.mdx`
- Create: `content/principles/pr-08-evolutionary-design.mdx`
- Modify: `content/principles/pr-01-information-hiding.mdx`
- Modify: `content/principles/pr-04-dip-ioc-dependency-injection.mdx`
- Modify: `content/principles/pr-05-composition-over-inheritance.mdx`
- Modify as generated: `src/generated/project-status.json`
- Modify as generated: `src/generated/source-ledger.json`
- Modify as generated: `src/generated/topic-indexes.json`
- Modify as generated: `src/generated/topic-manifest.json`
- Modify: source/document-count fixture tests listed in File Structure.
- Test: `tests/g007-batch1-content.test.mjs`
- Test: `tests/g007-batch2-content.test.mjs`

**Interfaces:**

- Consumes: Remaining governed citations and relation fixtures.
- Produces: All three published Batch 2 pages and deterministic generated projections with 73 content documents and 437 governed sources.

- [ ] **Step 1: Create PR-07 with the exact metadata**

Use:

```yaml
---
title: Fail Fast、Fail Safe 与 Graceful Degradation
slug: /principles/pr-07
content_type: principle
status: reviewed
difficulty: advanced
analyzed_at: 2026-07-28
source_cutoff: 2026-07-28
confidence: high
domains:
  - software-architecture
agent_patterns: []
protocols: []
quality_attributes:
  - reliability
  - safety
  - availability
tags:
  - Fail Fast
  - Fail Safe
  - Graceful Degradation
summary: 按错误可检测性、继续运行的危害、副作用可逆性和诚实降级能力选择故障响应。
topic_id: PR-07
priority: P0
depends_on:
  - PR-02
adjacent_topics:
  - PR-02
  - PR-04
  - QA-01
related_cases:
  - /cases/microsoft-multi-agent-reference-architecture
related_questions: []
---
```

- [ ] **Step 2: Write PR-07 around a hazard and side-effect decision table**

In `## 机制`, include:

```markdown
| 判断输入 | Fail Fast | Fail Safe | Graceful Degradation |
| --- | --- | --- | --- |
| 错误可检测且尚无副作用 | 明确拒绝当前操作并释放资源 | 若拒绝即安全状态，则保持拒绝 | 仅在替代结果仍真实时继续 |
| 继续运行可能造成安全、权限、资金或完整性伤害 | 停止受影响边界并暴露失败 | 转入已定义、可验证的安全状态 | 不适用，不能用可用性掩盖伤害 |
| 副作用已经发生且不可逆 | 返回失败还需核对、补偿或人工终态 | 隔离后续动作，保护残余不变量 | 只能降级未发生的非关键能力 |
| 依赖不可用但核心读取仍真实 | 快速拒绝依赖能力 | 保护写入和权限边界 | 标明陈旧度、缺失能力和恢复条件 |
| 无法区分健康与降级 | 暴露不可判定状态 | 保守停止或拒绝 | 不得静默返回看似完整的结果 |
```

Required corrections:

- Fail Fast is local and bounded; it is not “crash the entire process for every exception.”
- Fail Safe requires a named hazard and safe state; it is not silent error swallowing.
- Graceful Degradation must preserve truthful semantics and visible degraded state.
- One request path may fail fast at validation, fail safe at a harmful side-effect boundary, and degrade an optional presentation feature.
- Cite AWS and Google SRE as operational guidance, not universal thresholds.

Use the required fact/inference/site-analysis labels and link `/principles`, PR-02, PR-04, QA-01, and the Microsoft multi-agent reference architecture case.

- [ ] **Step 3: Create PR-08 with the exact metadata**

Use:

```yaml
---
title: 为演化设计
slug: /principles/pr-08
content_type: principle
status: reviewed
difficulty: advanced
analyzed_at: 2026-07-28
source_cutoff: 2026-07-28
confidence: high
domains:
  - software-architecture
agent_patterns: []
protocols: []
quality_attributes:
  - maintainability
  - compatibility
  - modifiability
tags:
  - 演化设计
  - 兼容窗口
  - 渐进迁移
summary: 用兼容窗口、可替换点、渐进迁移、遥测和退出条件把架构变化变成受控步骤。
topic_id: PR-08
priority: P0
depends_on:
  - PR-01
adjacent_topics:
  - PR-01
  - PR-04
  - PR-05
  - PR-06
  - MTH-03
  - MTH-04
related_cases:
  - /cases/aws-cell-shuffle-sharding
related_questions: []
---
```

- [ ] **Step 4: Write PR-08 around an explicit migration loop**

In `## 机制`, include:

```mermaid
flowchart LR
  A[识别易变决策与消费者] --> B[建立兼容窗口和可替换点]
  B --> C[在受限切片引入新路径]
  C --> D[观测正确性 负载与采用率]
  D --> E{证据满足退出条件?}
  E -->|否 可恢复| C
  E -->|否 不可接受| F[回滚或修订决定]
  E -->|是| G[迁移剩余消费者]
  G --> H[删除旧路径与兼容层]
  H --> I[记录证据并重新评估]
```

The prose must state:

- compatibility includes source, wire, and semantic behavior in the applicable API context;
- expand/migrate/contract creates a temporary compatibility window, not permanent dual support;
- replaceable seams need an identified volatile decision;
- telemetry must distinguish old and new path correctness, load, and adoption;
- old-path removal needs an owner and measurable exit condition;
- microservices, plugins, feature flags, and indirection do not automatically create evolvability;
- a big-bang replacement is not incremental migration.

Use visible links to PR-01, PR-04, PR-05, PR-06, MTH-03, MTH-04, `/principles`, and the AWS cell/shuffle-sharding case. Use the required fact/inference/site-analysis labels and visible links to all four governed sources.

- [ ] **Step 5: Add remaining reciprocal relations**

Update:

```yaml
# PR-04 adjacent_topics
  - PR-01
  - PR-05
  - PR-07
  - PR-08
```

Add visible PR-07 and PR-08 links in PR-04’s `## 相邻原则`, explaining that PR-07 locates failure policy at the correct control boundary and PR-08 uses dependency direction to replace policy during migration.

PR-01 and PR-05 already received PR-08 in Task 3. Confirm their visible relation text points to `/principles/pr-08`. Confirm PR-06 visibly links PR-08 and PR-08 visibly links PR-06.

- [ ] **Step 6: Regenerate content projections**

Run:

```bash
bun run generate:content
```

Expected projections:

- PR-06, PR-07, and PR-08 are `published: true`;
- `content_documents` is 73;
- `governed_sources` is 437;
- `completed_topics` remains 27 during Stage A;
- no PR-09 through PR-17 is published.

- [ ] **Step 7: Update only deterministic count fixtures**

Update these current-state expectations from 70 to 73 documents and from 431 to 437 sources:

```text
tests/content-review-health.test.mjs
tests/project-status.test.mjs
tests/source-ledger-pagination.test.mjs
tests/source-ledger-rendering.test.mjs
```

Keep the historical `431 个来源` evidence in `tests/g007-batch1-deployment.test.mjs`; it describes the exact Batch 1 deployment and must not be rewritten.

- [ ] **Step 8: Run targeted and generator gates**

Run:

```bash
bun test tests/g007-batch1-content.test.mjs \
  tests/g007-batch2-content.test.mjs \
  tests/source-ledger.test.mjs \
  tests/project-status.test.mjs \
  tests/content-review-health.test.mjs \
  tests/source-ledger-pagination.test.mjs \
  tests/source-ledger-rendering.test.mjs
bun run validate:content
bun run check:content
bun run check:links
bun run check:reviews
git diff --check
```

Expected: all commands exit 0; the Batch 2 test is GREEN for the first time.

- [ ] **Step 9: Commit the remaining Stage A content**

```bash
git add content/principles \
  src/generated \
  tests/g007-batch1-content.test.mjs \
  tests/g007-batch2-content.test.mjs \
  tests/content-review-health.test.mjs \
  tests/project-status.test.mjs \
  tests/source-ledger-pagination.test.mjs \
  tests/source-ledger-rendering.test.mjs
git commit -m "content: add failure and evolution principles"
```

---

### Task 5: Verify, review, integrate, and deploy exact Stage A

**Files:**

- Review: all Batch 2 commits and generated outputs.
- Do not modify `docs/content-backlog.md`.

**Interfaces:**

- Consumes: Complete Stage A branch.
- Produces: Exact Stage A SHA, successful Pages run, live route/asset/render/click evidence, and an independent review verdict.

- [ ] **Step 1: Run the complete repository gate**

Run:

```bash
bun run verify
git diff --check
git status --short --branch
```

Expected: all tests pass; 73 content documents and 437 sources validate; typecheck and production build pass; the worktree is clean.

- [ ] **Step 2: Perform independent editorial, source, and contract review**

Review PR-06, PR-07, and PR-08 separately for:

- the exact decision boundary in the approved spec;
- factual claims supported by the governed citation actually attached to the page;
- copyright and quotation scope;
- anti-overclaim language;
- deterministic representation;
- visible parent, adjacent, method/quality-attribute, and case links;
- no PR-09 through PR-17 route;
- test strength, including exact labels and page-specific misconception assertions.

Any Critical or Important finding blocks integration. Fix findings with targeted regression tests, rerun the full gate, and obtain a clean follow-up verdict.

- [ ] **Step 3: Push the feature branch and integrate into main**

```bash
git push -u origin codex/g007-principles-batch2
cd /Users/seal/projects/tego-arch
git fetch origin --prune
git merge --ff-only codex/g007-principles-batch2
bun run verify
git push origin main
git rev-parse HEAD
```

If `main` advanced independently, do not force the merge. Update the feature branch with current `main`, resolve only in-scope conflicts, rerun `bun run verify`, and then retry a fast-forward integration.

- [ ] **Step 4: Wait for the exact-SHA Pages run**

Use `gh run list` to find the `deploy.yml` run whose `headSha` equals the recorded Stage A SHA. Wait until:

```text
status=completed
conclusion=success
```

A successful run for another SHA is not deployment evidence.

- [ ] **Step 5: Run production route and asset smoke**

Require HTTP 200 for:

```text
https://sealday.github.io/tego-arch/principles
https://sealday.github.io/tego-arch/principles/pr-06
https://sealday.github.io/tego-arch/principles/pr-07
https://sealday.github.io/tego-arch/principles/pr-08
```

Extract one current production CSS and one current production JavaScript URL from deployed HTML and require HTTP 200 for both.

- [ ] **Step 6: Run browser render and real-click verification**

At desktop `1440x1000` and mobile `390x844`:

- require `document.documentElement.scrollWidth === document.documentElement.clientWidth`;
- ensure the PR-06/PR-07 tables remain inside an article-local scroll boundary;
- ensure the PR-08 Mermaid remains inside an article-local scroll boundary;
- require zero console warnings/errors after a clean navigation;
- click every visible Batch 2 adjacent route, all method/quality-attribute routes, and one case route per page;
- confirm rendered HTML contains no `/principles/pr-09` through `/principles/pr-17`;
- confirm visible source links include Fowler YAGNI, Pragmatic DRY, AWS failure guidance, Fowler Parallel Change, and Google AIP-180.

Record the exact number and category of successful clicks rather than estimating a total.

---

### Task 6: Close Batch 2 with deployment-backed Stage B evidence

**Files:**

- Create: `docs/reviews/g007-batch2.md`
- Create: `tests/g007-batch2-deployment.test.mjs`
- Modify: `docs/content-backlog.md`
- Modify as generated: `src/generated/project-status.json`
- Modify as generated: `src/generated/topic-indexes.json`
- Modify as generated: `src/generated/topic-manifest.json`
- Modify: `tests/knowledge-fixtures.test.mjs`
- Modify: `tests/project-status.test.mjs`

**Interfaces:**

- Consumes: Exact Stage A SHA, exact Pages run ID/URL, actual repository test count, live-smoke date, asset URLs, viewport observations, click matrix, and independent review from Task 5.
- Produces: Durable closure of only PR-06 through PR-08 while leaving G007 active.

- [ ] **Step 1: Create the review record from observed evidence**

Create `docs/reviews/g007-batch2.md` with the title `G007 Batch 2 Review and Deployment Evidence`, content author `codex-g007-principles-batch2`, reviewer `independent-code-reviewer-and-repository-contracts`, and live-smoke date `2026-07-28`.

Copy the exact runtime values from Task 5 into three visible lines:

- `Exact Stage A SHA:` followed by the 40-character SHA in backticks;
- `GitHub Pages run:` followed by the numeric run ID as a Markdown link to that run;
- `Exact run gate:` followed by the same SHA as `headSha`, then `status=completed` and `conclusion=success`.

Do not save the file until these are observed values rather than instructional text.

Add one subsection per page with editorial, fact, copyright, deterministic-representation, and anti-overclaim PASS findings grounded in Task 5. Add Runtime findings for routes, exact asset URLs, both viewports, local overflow, console, unpublished routes, exact click matrix, and source visibility.

Record the actual full repository result on a `Repository test gate:` line. Repeat the observed passed-test count on both sides of `/`; for example, an observed result of 445 passing tests is recorded as `445/445`, never as an estimated future count.

End with `Stage B closure — PASS` and state that G007 remains current, PR-09 through PR-17 remain incomplete, and the next batch begins at PR-09.

- [ ] **Step 2: Close exactly PR-06 through PR-08 in the backlog**

For each row, change `[ ]` to `[x]` and append:

- the exact Stage A commit link;
- the exact Pages run link;
- the canonical live route;
- the verified viewport/representation/source/relationship evidence.

Update the current release baseline to:

- 30 completed topics;
- 73 content documents;
- 437 governed sources;
- G007 still current;
- next batch PR-09 onward.

Keep durable story progress `6 / 20` and most recently completed parent story `G006`.

- [ ] **Step 3: Add deployment-closure tests**

Create `tests/g007-batch2-deployment.test.mjs`:

```js
import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';
import test from 'node:test';

const [review, backlog] = await Promise.all([
  readFile(new URL('../docs/reviews/g007-batch2.md', import.meta.url), 'utf8'),
  readFile(new URL('../docs/content-backlog.md', import.meta.url), 'utf8'),
]);

const shaMatch = review.match(/Exact Stage A SHA: `([0-9a-f]{40})`/u);
const runMatch = review.match(
  /GitHub Pages run: \[`(\d+)`\]\((https:\/\/github\.com\/sealday\/tego-arch\/actions\/runs\/\d+)\)/u,
);
const testGateMatch = review.match(/Repository test gate: `(\d+)\/\1` tests passed\./u);

test('records the exact successful G007 Batch 2 deployment', () => {
  assert.ok(shaMatch, 'review must record an exact 40-character Stage A SHA');
  assert.ok(runMatch, 'review must record the exact GitHub Pages run');
  assert.ok(testGateMatch, 'review must record a self-consistent passing test count');
  const [, sha] = shaMatch;
  const [, runId, runUrl] = runMatch;
  assert.equal(runUrl, `https://github.com/sealday/tego-arch/actions/runs/${runId}`);
  assert.match(review, new RegExp(`headSha=${sha}`, 'u'));
  assert.match(review, /status=completed/u);
  assert.match(review, /conclusion=success/u);
  assert.match(review, /Stage B closure — PASS/u);
  assert.match(review, /desktop `1440x1000`/u);
  assert.match(review, /mobile `390x844`/u);
  assert.match(review, /73 content documents/u);
  assert.match(review, /437 governed sources/u);
});

test('closes only PR-06 through PR-08 with the same deployment evidence', () => {
  const [, sha] = shaMatch;
  const [, runId] = runMatch;
  for (const id of ['06', '07', '08']) {
    const row = backlog
      .split(/\r?\n/u)
      .find((line) => line.startsWith(`- [x] **PR-${id} `));
    assert.ok(row, `PR-${id} must be checked`);
    assert.match(row, new RegExp(sha, 'u'));
    assert.match(row, new RegExp(`actions/runs/${runId}`, 'u'));
    assert.match(
      row,
      new RegExp(`https://sealday\\.github\\.io/tego-arch/principles/pr-${id}`, 'u'),
    );
  }
  assert.match(backlog, /^- \[ \] \*\*PR-09 /mu);
  assert.match(backlog, /- \*\*当前持久故事：\*\* `G007`。/u);
  assert.match(
    backlog,
    /- \*\*持久故事进度：\*\* 已完成 `6 \/ 20`；最近完成 `G006`。/u,
  );
  assert.match(
    backlog,
    new RegExp(`当前发布基线：\\*\\*[^\\n]*${sha}[^\\n]*${runId}`, 'u'),
  );
});
```

- [ ] **Step 4: Regenerate current completion projections**

Run:

```bash
bun run generate:content
```

Expected:

- PR-06 through PR-08 have complete backlog-projection status;
- PR-09 remains pending and unpublished;
- `completed_topics` is 30;
- `content_documents` is 73;
- `governed_sources` is 437.

Update `tests/knowledge-fixtures.test.mjs` so the principle fixture remains complete. Update the real-repository expectation in `tests/project-status.test.mjs` from 27 to 30 completed topics; keep 73 documents and 437 sources.

- [ ] **Step 5: Run the complete Stage B gate**

Run:

```bash
bun test tests/g007-batch1-content.test.mjs \
  tests/g007-batch1-deployment.test.mjs \
  tests/g007-batch2-content.test.mjs \
  tests/g007-batch2-deployment.test.mjs \
  tests/knowledge-fixtures.test.mjs \
  tests/project-status.test.mjs
bun run verify
git diff --check
git status --short --branch
```

Expected: all tests, content validation, generation check, link cache, review health, TypeScript, and production build pass.

- [ ] **Step 6: Commit and push Stage B**

```bash
git add docs/content-backlog.md \
  docs/reviews/g007-batch2.md \
  src/generated/project-status.json \
  src/generated/topic-indexes.json \
  src/generated/topic-manifest.json \
  tests/g007-batch2-deployment.test.mjs \
  tests/knowledge-fixtures.test.mjs \
  tests/project-status.test.mjs
git commit -m "docs: close g007 operational principles batch"
git push origin main
```

- [ ] **Step 7: Verify the exact Stage B deployment and final repository state**

Wait for the Pages run whose `headSha` equals the Stage B commit and require `completed/success`. Recheck:

```text
https://sealday.github.io/tego-arch/principles
https://sealday.github.io/tego-arch/principles/pr-08
```

Run:

```bash
git status --short --branch
git rev-parse HEAD
git rev-parse origin/main
```

Expected: clean `main`, identical local and remote SHA, successful exact Stage B deployment, and a clean preserved implementation worktree. Do not checkpoint G007.
